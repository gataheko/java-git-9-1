# java-git-9-1
Java console app demonstrating text file I/O operations. Implements CustomerDB class with getAll() and saveAll() methods to read/write customer data (first name, last name, email) from/to a tab-delimited text file using BufferedReader, BufferedWriter, and PrintWriter with try-with-resources statements.
